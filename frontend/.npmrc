; npm 项目级配置
; 让 CI 的 `npm ci` 与本地 `--legacy-peer-deps` 行为对齐，否则 npm ci 会严格 peer 校验。
;
; 根因：`@xterm/addon-canvas@0.8.0-beta.48` 的 peer 声明滞后于实际兼容性
;   - addon-canvas@0.8.0-beta.48 peer 声明：@xterm/xterm@^5.0.0
;   - 项目实际使用：@xterm/xterm@^6.0.0
;   - 运行时实测兼容（与已装的 @xterm/addon-webgl@0.19 同一发布代）
;
; 这是 npm 生态常见的 peer 声明滞后问题，npm 官方推荐 `legacy-peer-deps` 作为解法
; （告诉 npm 沿用 npm v6 的行为：peer 冲突只警告，不 fail install）。
;
; 影响范围：CI 的 `npm ci` 会自动读取本文件，与本地安装行为对齐；不改变产物。
legacy-peer-deps=true
