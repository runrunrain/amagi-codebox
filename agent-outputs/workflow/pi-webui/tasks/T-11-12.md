# 任务包 T-1.1+T-1.2（串行两阶段）：webui server 模块 + 历史合成（amagi-pi）

- 任务 ID：T-1.1 → T-1.2（蓝图执行计划 §3.2；先做完 1.1 验证再进 1.2）
- 工作目录：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`（分支 amagi/webui11）

## 必读（按序）

1. **契约（唯一权威）**：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui01/docs/webui-protocol.md`（v1.0.1a，408 行，全文通读）
2. **spike 参考实现（已验证代码）**：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui02/extensions/amagi-core/webui-spike/index.ts`
3. amagi-core 入口装配：worktree 内 `extensions/amagi-core/index.ts`（如何注册新模块；AMAGI_SUBAGENT 双路径）
4. JSONL 解析参考：worktree 内 `extensions/amagi-core/session-viewer.ts`
5. pi 事件类型：`/Users/maorun/.local/node/lib/node_modules/@earendil-works/pi-coding-agent/docs/session-format.md` 与 sdk.md 事件节

## T-1.1 目标：webui server 模块

`extensions/amagi-core/webui/`：`server.ts`（node:http + 手写 WS 或复用 spike 方案）、`events.ts`（白名单订阅+环形缓冲 4096+seq/floor）、`registry.ts`（**目录式 per-pid** `~/.pi/agent/amagi/webui-registry/<pid>.json`，AMAGI_WEBUI_PORT_FILE 可覆盖用于测试）、Host 校验、CORS（§6.4）、就绪 503→200、退出三级清理、bind 失败回退（§7.2）。实现程度对齐契约 v1.0.1a 的每条【0.2】/【M1】规则；/api/input 可留 405 stub（M2）。在 amagi-core 入口以开关接入（默认启用，保持 AMAGI_SUBAGENT 路径不加载 server）。

## T-1.2 目标：历史合成

`webui/history.ts`：JSONL 解析（复用 session-viewer 逻辑抽取）+ 进程内缓冲合成、entry cursor 空间、**revision 机制**（分支切换/compaction +1，纯追加不 +1；rev 参数校验；410 附 currentRevision）、分页 200、密钥字段递归过滤（token/secret/password/key 名，值→`••••`）。

## 验证（两阶段各自过再收尾）

- 单测：node:test 风格 `.test.mjs`（对齐仓内既有测试），覆盖：信封 seq 单调/floor、hello/bye、Host 403、CORS 预检、就绪 503、注册表原子写与目录式扫描淘汰、bind 回退、history cursor/revision/410/密钥过滤、缓冲溢出回源。用 stub ExtensionAPI（参考 spike test-stub.mjs）。
- worktree 缺 node_modules：先 `ln -s ../node_modules node_modules`（指向主仓）再 `npm test`；若既有失败集与主仓一致（对照法，参考 T-0.3 报告归因法）即无回归。
- 真实会话冒烟（可选但推荐）：复用 spike-0.2 的 run-spike 手法验证一轮。

## 允许修改

仅 worktree：`extensions/amagi-core/webui/**`（新增）、`extensions/amagi-core/index.ts`（装配点最小改动）、新增测试文件、node_modules 符号链接（gitignored）。

## 禁止修改

pi 本体、`~/.pi/agent/`、amagi-codebox 仓、主分支、既有 amagi-core 模块逻辑（session-viewer 只读复用；如需抽取公共函数，在 webui/ 内自建副本并注释来源）。

## 完成证据

返回：两阶段各一段结论（模块清单+关键实现决策+测试统计）+ 真实 `npm test` 输出摘要（新增用例数/既有失败对照归因）+ 改动文件清单。不粘贴大段代码。
