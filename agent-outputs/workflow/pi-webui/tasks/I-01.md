# 任务包 I-1：M1-GATE 跨仓 E2E 验收（codebox 壳 × amagi-pi 集成版）

- 任务 ID：I-1（蓝图执行计划 §3.2 收尾；M1-GATE）
- 双现场：codebox worktree `/Users/maorun/maorun-workpace/amagi-codebox/.amagi-worktrees/webui15`（壳）；amagi-pi 集成 worktree `/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`（server+页面）

## 必读

1. 验收口径：蓝图需求 §5 A-1/A-2/A-4（`/Users/maorun/maorun-workpace/Database/01-AI技术与工具/codebox优化/pi的webui实现/需求分析/pi的webui实现-需求分析.md`）
2. T-1.4 报告要点（A-6 已验，本次不重复）：同目录 `调研报告/spike-assets/a6-*.png` 存在性确认即可
3. 契约 §7.2（AMAGI_WEBUI_PORT env 注入）、T-1.6 的 stub 驱动手法（你自己上一轮的 wails dev + agent-browser 模式）

## 目标：真实跨仓 E2E（A-1 + A-2 只读 + A-4 复核）

**环境准备（受控例外，用后必还原）**：临时把 `~/.pi/agent/settings.json` 的 packages 中 amagi-pi 路径指向 `/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`（先备份原文件；验收后立即还原并 diff 确认零残留）。这样 codebox embedded 启动的 pi 会话加载集成版扩展（含 webui server）。

**A-1 切换不中断**（wails dev 真实 app，agent-browser 驱动）：
1. 启动 pi 会话（embedded，任一可用 provider/model）→ 等待工具栏出现"终端|网页"切换控件（真实探测 available，非 stub）；
2. TUI↔Web 切换 3 次；
3. 切回 TUI 后向会话输入一条指令（如 `请回复 ok`）确认会话存活、xterm 缓冲完好。

**A-2 历史+实时（只读档）**：
1. 会话先在 TUI 侧完成 1 轮对话；
2. 切到 Web：历史消息可见（该轮内容）；
3. 切回 TUI 再发 1 轮 → 切到 Web（或留在 Web 由另一驱动窗口）确认新轮次实时流式呈现。技术允许：TUI 输入用 PTY/CGEvent 合成，或第二窗口观察；以截图+日志佐证。

**A-4 复核**：还原 settings 指回主仓 amagi-pi（无 webui）后再启动一个 pi 会话 → 切换控件不出现、终端正常（真实无插件场景，补 T-1.6 stub 验证的最后一环）。

**两仓全量回归**（真实命令，记录输出）：
- amagi-pi webui11：`npm test`（预期 1026+）；`npm run build:webui` + `npm --prefix webui test`
- codebox webui15：`go vet ./... && go test ./... -count=1`；`npm --prefix frontend run build`

## 证据要求

- 截图 ≥5 张（切换控件出现/TUI 态/Web 态历史/Web 态流式/A-4 隐藏）落 `调研报告/spike-assets/i1-*.png`；
- 验收记录（逐项 pass/fail + 环境与命令输出摘要）落 `调研报告/spike-assets/i1-验收记录.md`；
- settings.json 还原 diff 证据（backup vs restored）。

## 允许修改

两 worktree 内的小修复（若 E2E 暴露 bug：直接修+记录+补测）；`/tmp` 临时脚本。

## 禁止修改

两仓主分支与主仓工作区、pi 本体、settings.json 除受控例外窗口外。

## 失败处理

E2E 级失败（如切换控件不出现）：先定位归因（probe 通道/扩展加载/端口），小修重试；语义级问题登记回 Leader 不硬改契约。

## 完成证据

返回：A-1/A-2/A-4 逐项结论 + 回归命令输出摘要 + 修复清单（若有）+ 证据路径清单。
