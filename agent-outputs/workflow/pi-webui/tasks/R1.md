# 修复批次 R1：M1 审核问题（diting 65e2c908：1C+3M+3Minor）

- 背景：M1-GATE E2E 通过但 diting 阶段审核 BLOCK（Critical 跨源读取 + 探测误采纳/积压 + 分页边界）。两任务并行、零文件交集。
- 材料原报告：worktree amagi/webui01 内契约；审核结论已逐条内嵌各任务包。

## 任务包 R1-A（laojun，worktree webui11 + webui01）：Critical 鉴权 + Minor5/7

范围：契约 v1.0.2 + server.ts capability 鉴权 + registry 淘汰接线。
1. 【Critical】修订契约（webui01/docs/webui-protocol.md → v1.0.2）：新增每会话高熵 capability（≥128bit 随机）；通道：codebox 生成后经 env `AMAGI_WEBUI_TOKEN` 注入扩展；扩展经 iframe fragment（`#/t=<token>`，fragment 不入 HTTP 日志）传给页面；API 校验 `Authorization: Bearer <token>`、WS 握手校验 `Sec-WebSocket-Protocol: webui, <token>` 或等价机制；静态资源仍 ACAO:* 无鉴权但页面 JS 凭 fragment 取 token——完整性核对：确认 fragment 方案在 sandbox iframe（allow-scripts, opaque origin）真实可行（T-1.4 已证 opaque origin 下 fetch 可行；fragment 经 iframe src 传递不受 sandbox 影响）。跨源无 token 请求 → 403。同时收窄 CORS：`/api/*` 不再 ACAO:*（同源+sandbox 页面携带 token 直连，无需通配跨源）——若影响 sandbox 静态模块加载，静态资源维持 ACAO:* 可接受（无鉴权数据）。
2. 【Critical】server.ts：实现上述校验（API+WS 全端点）；AMAGI_WEBUI_TOKEN 缺失时（独立终端用 A-6 场景）生成并写入注册表条目（token 字段），浏览器直连路径从注册表文件拿 token 拼 fragment——更新 README。
3. 【Minor5】页面 http-source.ts：v!==1 一律关 WS → 明确 degraded（v>1 提示客户端过旧）。
4. 【Minor7】注册表 best-effort 淘汰落实到生产消费方：删 TS 侧未接线 scanner 或接线均可，但 Go 侧（codebox 仓）不改（归 R1-B）；本任务只保证 TS 侧与测试不误导。
5. 测试：恶意 Origin/无 token 403 用例、fragment→Bearer 链路、token 缺失回退链、v!==1 断连。
验证：npm test 全量 + vitest + build:webui；契约变更记录 v1.0.2。

## 任务包 R1-B（luoshen，worktree webui15）：Major2/3/6 + 契约对齐消费

范围：codebox 探测健壮性 + 页面分页边界 + 生命周期。
1. 【Major2】probe/service：首次采纳强校验 info.PID==tracker.pid && sessionId 非空 && info.port==candidatePort；粘性后 PID+sessionId 双校验；测试补端口复用/他服务占用场景。
2. 【Major3】503 处理：仅 v/PID 校验通过的 503 视为未就绪；否则注册表回退；每候选独立超时；前端 ensureWebUIProbe 单飞（await 链）；Go 网络 I/O 移出全局锁（收窄临界区）。
3. 【Major4】http-source 分页：最新页 since=max(0,total-limit)；向前分页按剩余量；补 total=201/205/398/399 与多页测试（用真 HTTP 算法驱动，非 mock 绕过）。
4. 【Minor6】WebUI Remove 接入 App.RemoveSession/批量清理 commit 点；前端 generation/abort 丢弃迟到结果。
5. 契约 v1.0.2 消费（R1-A 并行冻结，按任务包所述语义实现）：env 注入 token、OpenWebPlane 返回 URL 含 fragment、探测不携带 token（info 允许无鉴权？——否：info 也带 token，Go 侧从 env/registry 读 token 供探测）。
验证：go vet+go test ./... + frontend build + 针对性新测试；不要求重跑 wails dev E2E（R2 批次统一复验）。

## 共同纪律
修改全部落盘；不 commit；不改范围外文件；返回结论+文件清单+测试摘要。两包零文件交集（R1-A：amagi-pi 双 worktree；R1-B：codebox worktree）。
