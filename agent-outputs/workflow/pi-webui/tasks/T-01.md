# 任务包 T-0.1：webui 协议契约 v1 定稿（吸收 spike 结论，冻结暂定值）

- 任务 ID：T-0.1（蓝图执行计划 §3.1；你是契约单一所有者）
- 工作目录：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui01`（独立 worktree，分支 amagi/webui01）

## 必读（按序）

1. 蓝图技术方案：`/Users/maorun/maorun-workpace/Database/01-AI技术与工具/codebox优化/pi的webui实现/技术实现/pi的webui实现-技术实现方案.md` §5 信封/领域模型、§7 契约摘要、§2 决策表
2. 三份 spike 报告（同目录 `调研报告/`）：
   - `spike-0.2-server.md` ——重点其"7 条 M0 契约影响建议"与实测帧样例（信封字段以实测为准）
   - `spike-0.3-input.md` ——/api/input 契约依据（sendUserMessage、deliverAs 硬条件、错误路径取证）
   - `spike-0.4-iframe.md` ——sandbox 约束对页面侧的要求
3. spike 参考实现（证据最实）：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui02/extensions/amagi-core/webui-spike/index.ts`（已验证的信封/缓冲/Host 校验/就绪探测/退出清理实现）
4. pi 会话格式（history 端点 entry 映射依据）：`/Users/maorun/.local/node/lib/node_modules/@earendil-works/pi-coding-agent/docs/session-format.md`

## 目标（可验收）

产出 `docs/webui-protocol.md`（amagi-pi 仓，worktree 内）：webui 协议 v1 完整契约，作为两仓唯一公共契约冻结，M0-GATE 的核心交付。

## 契约必须覆盖

1. 版本与兼容：协议版本字段（v:1）、信封 schema、版本协商/前向兼容规则。
2. 端点全量：GET /api/info（503→200 就绪语义、字段表）、GET /api/history（cursor/since/limit/分页、410 过期、entry 类型映射表）、WS /ws/events（信封、seq 单调、`?since=` 重连回放+续传、断开码）、POST /api/input（M2：text/mode=prompt|steer、202/405/409、**streaming 期注入必须 deliverAs:"steer" 的服务端自动处理规则**、无 deliverAs 时的 4xx 显式错误——吸收 T-0.3 教训：禁止假 200）。
3. 事件订阅白名单：透传哪些 pi 事件类型（含 message_update delta 语义）、不透传什么。
4. 安全规则：127.0.0.1 bind、Host 白名单、sandbox=allow-scripts 下页面侧约束（无 localStorage 依赖等，吸收 T-0.4）、密钥字段过滤（token/secret/password/key）。
5. 生命周期：端口发现（AMAGI_WEBUI_PORT env + 注册表回退双通道格式）、就绪探测、进程退出清理、注册表原子写格式。
6. **冻结 4 个暂定值**（给出定值与理由）：事件环形缓冲容量（参考 spike 实现值）、历史分页大小、input 队列上限、（marked 版本这项留给 T-1.3 实现时锁版，契约中标注归属）。
7. 错误码总表 + 最小客户端实现指引（重连=refetch 模式）。

## 实现要求

- 文档即契约：JSON 示例字段名与 spike 实测帧一致；不引入 spike 未验证的行为（若契约超出 spike 验证范围，显式标注"M1 首验"）。
- 中文撰写，结构清晰可审。

## 允许修改

仅 worktree 内新增 `docs/webui-protocol.md`（不改其他文件）。

## 验证与完成证据

- 任务结果返回：契约要点摘要（≤10 行）+ 文件路径 + 4 暂定值冻结结果。
- 后续由 Leader 安排 diting-quick 快审（你无需自审）。

## 失败处理

spike 报告间若有矛盾（如端口发现细节），以 T-0.2 实测为准并在契约"开放问题"节登记。
