# 任务包 T-2.4：M2-GATE 全量 E2E（输入通道跨仓 + 回归终验）

- 任务 ID：T-2.4（蓝图执行计划 §3.3 收尾；M2-GATE）
- 双现场：codebox `/Users/maorun/maorun-workpace/amagi-codebox/.amagi-worktrees/webui15`；amagi-pi `/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`
- 手法：复用 I-1/R2-E（wails dev + settings.json 受控例外：备份→指向 webui11→还原+diff/md5 证据）

## 验收清单

**M2 核心场景（iframe sandbox 内输入通道，首验）**：
1. M2-1：Web 平面输入框 idle 发送 prompt → 202 → 轮次触发 → Web 实时流式呈现 → 切回 TUI 该轮完整可见（A-2 全量 + FR-5.2）。
2. M2-2：streaming 期发送（steer）→ 排队 badge/pendingCount → 本轮结束消费 → 新轮呈现（TUI 同步可见）。
3. M2-3：行内错误路径（如超 128KiB 或构造 409 场景若可控）：错误行内呈现不弹窗、连接不中断。
4. a11y/性能抽查：切换控件 aria-pressed、输入区键盘操作（Tab+Enter 发送）、大历史首屏体感（t23 数据复述即可）。

**快速回归**：A-1（切换不中断，抽 1 轮）、A-4（还原 settings 后控件隐藏）。

**两仓全量回归（真实命令，输出摘要）**：
- amagi-pi webui11：npm test（预期 1032+）+ vitest + tsc + build:webui
- codebox webui15：go vet + go test ./... -count=1 + npm --prefix frontend run build

**证据**：截图 ≥4 张（M2-1 发送前/流式中、M2-2 排队 badge、TUI 侧可见）落 `调研报告/spike-assets/t24-*.png`；在 `i1-验收记录.md` 追加「T-2.4 M2-GATE」节。

## 纪律
小 bug 修（worktree 内）+记录；语义级登记；返回逐项结论+回归摘要+证据路径。
