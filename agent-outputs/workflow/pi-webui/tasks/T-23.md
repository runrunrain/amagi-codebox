# 任务包 T-2.3：性能、a11y、令牌快照核对 + 前缀重复排查（M2 收尾实现任务）

- 任务 ID：T-2.3（蓝图执行计划 §3.3）
- 双现场（单节点串行两半，均为 Leader 授权写入范围）：
  - amagi-pi 页面：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`（webui/ 页面子项目；**仅允许改 webui/ 与其测试**，extensions/ 不碰）
  - codebox 壳：`/Users/maorun/maorun-workpace/amagi-codebox/.amagi-worktrees/webui15`（前端壳组件）

## 必读

1. 交互文档 §8/§9（a11y 与性能预算）：`…/pi的webui实现/视觉交互/pi的webui实现-前端视觉交互设计.md@v2.0`
2. 视觉风格 §10（令牌快照同步规则）
3. codebox 令牌源：`/Users/maorun/maorun-workpace/amagi-codebox/frontend/src/styles/tokens.css`
4. T-2.1 报告的范围外风险：M1 live 流式 `message_update` 短暂前缀重复（projects-memory/…/20260815-181515-pi-webui-m2-input-channel/optimization-report.md 内记录）

## 任务清单

### A. 页面半（webui11/webui/）
1. **性能实测（NFR-3）**：用 mock 大会话（large 剧本已存在）+ 真实 HistoryServer 构造 ≥1000 条记录，量测首屏（loading_history→live）时长 ≤3s；流式批渲染 ≤1 帧/50ms（performance.now 打点）；数据落 `调研报告/spike-assets/t23-perf.md`。超预算则优化（分页预取/渲染批合并）并复测。
2. **前缀重复排查（T-2.1 登记）**：定位 live message_update 快照替换时序中短暂重复的根因（疑：快照替换与增量渲染窗口/或 history 终态与 live 帧竞态残留）；修复+回归测试；若根因在 pi 事件流本身（服务端行为），修客户端幂等消费并在报告说明。
3. **a11y 核查**：键盘全程可达（Tab/Enter/Space、折叠 aria-expanded、FAB）、aria-live=polite 仅终态播报、prefers-reduced-motion 8 类动效降级实测（emulate media）；逐项记录。
4. **令牌快照漂移核对**：webui-tokens.css vs codebox tokens.css 逐值 diff；漂移则同步并更新文件头版本注释。

### B. 壳半（webui15 前端）
5. 切换控件 aria-pressed、WebPlaneHost iframe title、错误/结束态文案可读性；vue 组件测试补一条 a11y 断言（现有测试文件内追加）。

### 验证
- 页面：vitest+tsc+build:webui 全绿（含新增回归）。
- 壳：`npm --prefix frontend run build` + go test ./internal/webui/ 不受影响。
- 全部命令真实输出摘要落返回值；性能数据与 a11y 清单落 t23-perf.md。

## 完成证据
返回：两半结论+性能数据表+a11y 核对表+前缀重复根因与处置+改动清单。
