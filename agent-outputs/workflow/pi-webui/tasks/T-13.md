# 任务包 T-1.3：webui 会话页面 v1（amagi-pi，契约 mock 起步）

- 任务 ID：T-1.3（蓝图执行计划 §3.2）
- 工作目录：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui13`（分支 amagi/webui13）

## 必读（按序）

1. **契约（接口权威）**：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui01/docs/webui-protocol.md`（v1.0.1a；重点 §3 信封/hello/floor、§4.2 history/revision、§6.5 基址推导与 iframe src、§10 客户端实现指引——你的实现剧本）
2. **视觉规格（样式权威）**：`/Users/maorun/maorun-workpace/Database/01-AI技术与工具/codebox优化/pi的webui实现/视觉交互/pi的webui实现-视觉风格.md`（v2.0：色彩角色矩阵/字型/组件解剖/令牌快照策略 §10）
3. **内容显示规格**：`/Users/maorun/maorun-workpace/Database/01-AI技术与工具/codebox优化/pi的webui实现/视觉交互/pi的webui实现-前端视觉交互设计.md` v2.0 §4.3/§4.4（Markdown 白名单/代码块三色/截断/时间戳/流式/空态）
4. **设计稿（对照基准）**：同目录 `assets/v2/webui-main-v2.svg`、`assets/v2/anatomy-v2.svg`、`assets/v2/toolbar-toggle-v2.svg`
5. codebox 令牌源：`/Users/maorun/maorun-workpace/amagi-codebox/frontend/src/styles/tokens.css`

## 目标（可验收）

`webui/` 子项目：Vite + vanilla TS + marked（实现时锁版写入 package.json，契约移交项）。交付：

1. `webui/src/webui-tokens.css`：tokens.css 的 1:1 值快照（仅用到的子集，文件头注明源版本与同步规则）。
2. 页面组件树（无框架或最小组件化）：页头（连接点/元信息/M1"在终端平面输入"提示）/ 消息流（用户块+发话者竖条、助手 Markdown 白名单渲染+HTML 转义、思考三态、工具卡片四态+嵌套引导线+深度折叠、轮次时间戳分隔、中断标记）/ 代码块（材质回声+三色高亮+语言角标+20 行折叠）/ 滚动（底锚跟随+回到底部 FAB）/ 状态机（init→loading_history(骨架线)→live(streaming|idle)→degraded|ended）/ 重连协议（§10：快路径条件+缺口回退+refetch）/ 空态。
3. 数据层：mock 驱动可独立开发——实现 `DataSource` 抽象（HttpDataSource 按契约 §10 时序；MockDataSource 提供全元素剧本：流式/中断/失败卡/多轮/大会话分页）；URL `?mock=1` 切换。
4. 构建：`npm run build`（webui 子项目）产物输出到契约规定位置（服务端 static 服务路径 `/`——与 T-1.1 节点约定的目录：`extensions/amagi-core/webui/static/`，构建脚本输出指过去；本仓不提交产物的话在 README 注明构建步骤，产物目录加入 .gitignore 之外按仓内习惯处理——**产物提交**，因为 codebox/独立浏览器直接服务需要）。
5. 冒烟测试：vitest + happy-dom（子项目自有 devDeps），覆盖：Markdown 白名单/转义、思考三态、工具卡状态、revision 410 触发 refetch、floor 缺口触发全量、mock 剧本渲染全元素。

## 视觉硬约束（违反即返工）

零手写十六进制（只引快照令牌）；字号四档 12/13/14/16；reduced-motion 全部动效降级；aria-expanded/aria-live 按交互文档 §8。

## 允许修改

仅 worktree：`webui/**`（新增子项目，含 package.json/vite.config/独立 tsconfig）、根 package.json 仅可加 workspace 无关的独立 script（如 `"build:webui"`），不引入根依赖变更。

## 禁止修改

extensions/ 既有代码、amagi-codebox 仓、pi 本体、主分支。

## 完成证据

返回：模块结构清单+关键决策（marked 版本号）+ 与设计稿 webui-main-v2.svg 的逐项对照清单（元素→组件→状态）+ 构建与测试真实输出摘要。截图不要求（I-1 集成时统一验）。
