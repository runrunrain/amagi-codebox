# 任务包 T-2.1+T-2.2：输入通道（server /api/input + 页面输入区）

- 任务 ID：T-2.1 → T-2.2（蓝图执行计划 §3.3；M2，T-0.3 已证 sendUserMessage 强档位可行）
- 工作目录：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`（集成现场，含 R1 鉴权基线）

## 必读

1. 契约 v1.0.2（接口权威）：分支 amagi/webui01 `docs/webui-protocol.md`（worktree 已清理，读法：`git -C /Users/maorun/maorun-workpace/amagi-pi show amagi/webui01:docs/webui-protocol.md`）——§4.4 /api/input（202/405/409 三态、deliverAs 自动补、防假 200）、§5 queue 载荷处置（M2 冻结义务）、§4.1/§3 pendingCount 真实计数
2. spike-0.3 注入语义：`调研报告/spike-0.3-input.md`（sendUserMessage、idle 立即/streaming 排队、deliverAs 硬条件）
3. 现场实现：`extensions/amagi-core/webui/server.ts`（405 stub 位）、`webui/src/`（controller/http-source/doc/render，输入区挂载点）

## T-2.1 目标：/api/input 服务端实现

- 实现 405 stub → 真实端点：Bearer 鉴权、`{text, mode}` 校验（400）、idle→`sendUserMessage` 202 delivered:"now"、streaming+steer→自动补 deliverAs 202 "queued"、streaming+prompt→409 agent_streaming、队列计数≥16→409 queue_full（契约冻结值）；128KiB 上限。
- pendingCount 真实计数：queue 事件驱动 + 202 响应联动，/api/info 与 hello 同步。
- **queue 载荷首验与冻结**（契约 M2 义务）：实测 pi queue 事件载荷形态 → 按实冻结 schema 写入契约 §5（v1.0.3 小版本，变更记录注明）；页面消费实现。

## T-2.2 目标：页面输入区（M2 档）

- 输入区组件（视觉风格 §7.7：card 底/hairline 上边线/焦点环 accent/发送按钮；多行输入）。
- 三态：发送（idle：mode prompt）/排队中（streaming：mode steer，badge）/禁用（degraded/queue_full）；状态源=agent 事件+pendingCount+202 快照。
- 注入后用户气泡即时呈现（input 事件白名单已含）；409/400 错误以行内提示呈现（不弹窗）。
- a11y：输入框 label、发送按钮 aria-disabled、错误 aria-live=assertive。

## 验证

- server 单测：三态/队列上限/鉴权失败 403/防假 200 回归/128KiB/计数同步。
- 页面 vitest：三态切换、注入气泡、错误呈现、queue 载荷解析（用首验真实形态）。
- 真实会话冒烟（一次）：idle 注入 + streaming steer 排队各一轮，浏览器观察（截图落 `调研报告/spike-assets/m2-*.png`）。
- 全量：npm test + vitest + tsc + build:webui。

## 允许修改

worktree 内：webui/ 模块、webui/ 页面、tests/、契约文件（git show 读原版后，在 webui01 worktree 重建再改，或直接在 webui11 内以 docs/ 副本变更后由 Leader 合回——选前者：`git -C /Users/maorun/maorun-workpace/amagi-pi worktree add .amagi-worktrees/webui01 amagi/webui01` 重建后编辑）。

## 完成证据

返回：两段结论+queue 载荷冻结结果+测试摘要+截图路径。
