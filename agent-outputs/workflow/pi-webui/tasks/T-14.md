# 任务包 T-1.4：A-6 独立浏览器验收 + amagi-pi README（集成现场）

- 任务 ID：T-1.4（蓝图执行计划 §3.2；LaneA 收尾）
- 工作目录：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui11`（集成现场：server 模块 + webui/ 子项目 + static 产物均已合入，npm test 1025/1025 绿）

## 必读

1. 契约：`/Users/maorun/maorun-workpace/amagi-pi/.amagi-worktrees/webui01/docs/webui-protocol.md`（v1.0.1a）
2. spike-0.2 的加载与驱动手法：`/Users/maorun/maorun-workpace/Database/01-AI技术与工具/codebox优化/pi的webui实现/调研报告/spike-0.2-server.md`（`pi -e <path>` 加载、run-spike 脚本模式）
3. worktree 内实现：`extensions/amagi-core/webui/`（server/events/registry/history）、`extensions/amagi-core/index.ts`（装配点，看 webui 模块如何被启用/是否需要开关）、`webui/`（页面）、`tests/webui-*.test.mjs`

## 目标（可验收）

**A-6 端到端**：纯终端方式运行真实 pi 会话（扩展加载本 worktree 的 amagi-core，含 webui server），从注册表拿到端口，浏览器打开 `http://127.0.0.1:<port>/`：
1. 页面加载成功（静态服务 + 基址推导生效）；
2. 会话历史渲染（页面 /api/history 拉到本轮之前的消息）；
3. 在 TUI 终端里发起新轮次 → 页面实时流式呈现（WS 事件流）；
4. 页面控制台无未捕获错误（sandbox/CORS 契约 §6 项首验）。

**README**：worktree 内 `webui/README.md`（或根 README 增补节，选更合仓内习惯的）：webui 是什么、构建（npm run build:webui）、独立使用方法（启动 pi 会话→查注册表→浏览器打开）、?mock=1 调试模式、令牌快照同步规则提示。

## 实现要求

- 加载方式优先 `pi -e <worktree>/extensions/amagi-core`（零 settings 改动；若 index.ts 装配需要开关/参数，读实现确认后如实使用）。注意本机 ~/.pi/agent 已注册主仓 amagi-pi 包——用 `--no-extensions` 或等效隔离避免双加载冲突（参考 spike-0.2 手法）。
- 浏览器验证可用 agent-browser（自动化）或手动；截图 ≥3 张（初始+历史+流式中）落 `/Users/maorun/maorun-workpace/Database/01-AI技术与工具/codebox优化/pi的webui实现/调研报告/spike-assets/a6-*.png`。
- 发现契约【M1】标注项与实际行为不符时：小偏差直接修实现（worktree 内）并在报告记录；语义级偏差不动契约，报告里登记交 Leader。
- 会话成本控制：单轮短 prompt ×2。

## 允许修改

仅 worktree 内：README、实现文件的小修复、测试补充。

## 禁止修改

pi 本体、`~/.pi/agent/settings.json`、amagi-codebox 仓、主分支、契约文档。

## 完成证据

返回：A-6 四步逐项结果（含截图路径）+ 发现并修复的问题清单（若有）+ 契约首验结论（§6 CORS/§6.5 基址/静态 MIME 是否如契约工作）+ README 路径。
